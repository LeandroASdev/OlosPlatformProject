import SideBar from "@/components/sidebar";

const SysConfiguration = () => {
  return (
    <div className="flex min-h-screen">
      <div className="transition-all duration-300 ease-in-out">
        <SideBar />
      </div>
    </div>
  );
};

export default SysConfiguration;
